# Copy to `.env` (gitignored) and set your own path. Never commit `.env`.
# Docker Desktop on Windows: use forward slashes, e.g. C:/Users/YOU/Documents/workflows

WORKFLOWS_HOST_PATH=C:/path/to/your/workflows
